package LC3578;

public class Solution {
    public int countPartitions(int[] nums, int k) {
        // Dynamic Programming
        // dp[index]: the number of ways to partition the nums[0 ... index]
        // base case: dp[0] = 1
        return 0;
    }
}
